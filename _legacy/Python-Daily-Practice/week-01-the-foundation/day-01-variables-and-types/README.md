# Day 01: Variables and Types

> every journey starts with a single variable — today you'll meet Python's building blocks

**Concepts:** variables, types, type conversion, isinstance | **Difficulty:** ★☆☆☆☆ | **Time:** ~20 min
**Builds On:** —

## Learning Objectives

By the end of this lesson, you will be able to:
- Understand Python's fundamental data types (int, float, str, bool, None)
- Create variables and understand variable naming rules
- Convert between types using int(), float(), str(), bool()
- Use isinstance() to check types dynamically

## Your Task

Build five functions that work with Python's core data types. You'll classify values by type, safely convert strings to integers, swap values, construct a profile dictionary, and calculate basic statistics from a list of numbers.

## How to Work

This daily practice uses a two-step workflow to balance exploration with professional standards:

1. **Draft & Explore** in `exercises.ipynb`: Use the Jupyter Notebook as your interactive scratchpad. It's designed for visual feedback and rapid iteration.
2. **Formalize** in `solution_template.py`: Once your logic is solid, copy your functions into the Python script. This file is your "production" code.
3. **Verify** with the test suite:
   ```bash
   pytest test_solution.py -v
   ```
   **Goal:** Turn all failing tests green. Success reveals the expert model answers!
