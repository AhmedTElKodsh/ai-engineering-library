# AI Engineering Library local environment
#
# Most Course 1 lessons run with plain Python, fixtures, and tests. API keys are
# optional and only needed when a later lesson explicitly asks for a real model
# provider instead of a fake or local boundary.

# Optional model provider keys for later modules
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Safe local defaults
AI_ENGINEERING_ENV=local
LOG_LEVEL=INFO

# Never commit real secrets.
